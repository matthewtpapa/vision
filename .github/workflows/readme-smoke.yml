name: readme-smoke

on: pull_request

jobs:
  readme-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -U pip setuptools wheel
      - run: pip install -e . numpy
      - run: python scripts/smoke_readme.py
